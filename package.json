{
  "name": "clima-search",
  "version": "1.0.0",
  "description": "A simple app, to search climate in a city",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "@viniolimpio3",
  "license": "ISC",
  "dependencies": {
    "commander": "^6.0.0",
    "node-fetch": "^2.6.0"
  }
}
